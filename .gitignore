# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# ---- Virtual environments ----
# .venv/ was committed by an earlier contributor and is broken on any other machine
# (it hard-codes C:\Users\SidikRoyale\...\python.exe). Untracked via
# `git rm -r --cached .venv` - the directory is still on disk, just no longer in git.
.venv/
.venv-ml/
.venv-ml313/
venv/
env/

# ---- ML artefacts ----
models/                 # trained joblib models (regenerated by scripts/retrain_ml.py)
data/external/          # downloaded third-party corpora (OTRF, etc.)
data/interim/           # intermediate ETL output
ml_train.db             # offline training database (rebuildable)
ml_train.db-wal
ml_train.db-shm
reports_ml/figures/     # generated evaluation figures

# ---- Runtime / local state ----
*.db-wal
*.db-shm
spool/
spool_test/
agent/spool*/

# ---- Duplicate clone accidentally nested inside this working tree ----
# ATOR/ is a second clone of this same repo (origin/main). Ignored, not deleted.
ATOR/
